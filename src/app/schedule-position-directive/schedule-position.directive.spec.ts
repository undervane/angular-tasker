import { SchedulePositionDirective } from './schedule-position.directive';

describe('PositionDirective', () => {
  it('should create an instance', () => {
    const directive = new SchedulePositionDirective();
    expect(directive).toBeTruthy();
  });
});
