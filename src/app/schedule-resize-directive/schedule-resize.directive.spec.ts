import { ScheduleResizeDirective } from './schedule-resize.directive';

describe('ScheduleResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ScheduleResizeDirective();
    expect(directive).toBeTruthy();
  });
});
